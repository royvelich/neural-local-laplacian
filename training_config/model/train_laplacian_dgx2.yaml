module:
  _target_: neural_local_laplacian.modules.laplacian_modules.LaplacianTransformerModule
  input_dim: 3
  d_model: 768
  nhead: 8
  num_encoder_layers: 1
  dim_feedforward: 768
  num_eigenvalues: ${globals.num_eigenvalues}
  dropout: 0.2
  normalize_loss_weights: False
  normalize_patch_features: True
  scale_areas_by_patch_size: True
  input_projection_hidden_dims: null
  output_projection_hidden_dims: null

  detach_area_head: true
  use_uniform_mass: false

  area_activation: bounded_sigmoid
  area_bound_C: 2.0

  mcv_mode: 'diagonal_gram'
  # 'diagonal_gram'    : s_ij = ‖g_ij‖²              (derived from grad head)
  # 'full_gram'        : s_ij = -⟨g_ii, g_ij⟩       (derived; FEM-correct,
  #                       can be negative when cross-Gram terms dominate)
  # 'learned'          : s_ij from an independent scalar head, signed
  # 'learned_positive' : same, with softplus → s_ij ≥ 0 (PSD-by-construction)
  # The two 'learned' modes decouple stiffness from gradient magnitude;
  # the model gains a dedicated head for cotangent-style weights.  Pair
  # with a soft consistency loss if you want the assembled Laplacians
  # from the two heads to track each other.
  stiffness_mode: 'diagonal_gram'
  normalize_grad_by_sqrt_k: true

  val_laplacian:
    assembly: diagonal_gram
    pruning: none

  fmap_val_cfg:
    k: 15
    num_eigenvectors: 100
    max_vertices_val: 5000
    eval_laplacians:
      - assembly: diagonal_gram
        pruning: none
        area_weighted: false
    geo_cache_dir: ./geo_cache
    use_geomfum_eval: true
    geomfum_descriptors: [hks, wks]
    geomfum_zoomout: false
    geomfum_zoomout_k_init: 20
    geomfum_zoomout_k_final: 50
    geomfum_zoomout_n_iters: 10
    fmap_lmbda: 1.0
    fmap_resolvent_gamma: 1.0

  feature_extractor: null
  loss_configs:
    - _target_: neural_local_laplacian.modules.losses.LossConfig
      loss_module:
        _target_: neural_local_laplacian.modules.losses.DirectionCosineLoss
      weight: 1.0
    - _target_: neural_local_laplacian.modules.losses.LossConfig
      loss_module:
        _target_: neural_local_laplacian.modules.losses.AreaOnlyLogMagnitudeLoss
      weight: 2.0
#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.LogMagnitudeLoss
#      weight: 1.0


#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.AreaOnlyLogMagnitudeLoss
#      weight: 0.1

#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.DirectionCosineLoss
#      weight: 1.0
#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.AreaOnlyLogMagnitudeLoss
#      weight: 2.0

#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.DirectionCosineLoss
#      weight: 1.0
#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.StiffnessActionLoss
#      weight: 1.0
#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.TangentPlaneProjectorLoss
#      weight: 10.0

#    - _target_: neural_local_laplacian.modules.losses.LossConfig
#      loss_module:
#        _target_: neural_local_laplacian.modules.losses.ProjectorRegularizationLoss
#        w_symmetry: 1.0
#        w_idempotency: 1.0
#        w_trace: 0.1
#      weight: 10.0